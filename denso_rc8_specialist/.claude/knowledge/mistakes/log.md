# mistakes/log.md
# 错误案例库 — 只增不删

> 每条错误必须包含根本原因分析（深挖，不接受表面原因）
> 格式说明见 knowledge/CLAUDE.md

---

## 使用说明

- 任务中发生错误，人工校正后触发写入
- 按时间顺序追加，不修改旧条目
- 搜索时按标签筛选（Ctrl+F 搜索 #标签）
- 已写入 patterns/ 的条目，在末尾注明关联规则

---

## 错误条目

<!-- 首次部署时为空，随任务积累 -->
<!-- 格式示例：

## [ERR-001] 错误标题
**日期**：2024-01-15
**任务类型**：codegen
**标签**：#robot #syntax #pacscript
**场景**：生成 DENSO RC8 运动程序时，用户要求 MoveL 指令
**错误表现**：输出了 MoveL 指令，但 PacScript 中直线插补使用 Move L（有空格），导致语法错误
**根本原因**：未查阅 agent_ref/controllers/ 手册直接输出，依赖训练知识中的通用机器人语法，
  而 PacScript 与 KUKA KRL / FANUC TP 语法不同
**解法**：Move L, P1, S=500 （L 与 Move 之间有空格，为插补模式参数）
**预防机制**：生成任何机器人代码前，必须先读取对应控制器手册确认语法
**是否已写入patterns**：是 [PAT-001]
---

-->
